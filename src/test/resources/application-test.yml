spring:
  application:
    name: credit-application

  # JPA configuration for test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  # Logging configuration
  logging:
    level:
      root: INFO
      com.creditapp: DEBUG

server:
  port: 0